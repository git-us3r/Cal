Stories:
1. Add button to stats card to view total times (this should be the default view if there are any times, otherwise it should have a cool img)
2. Add button to chart to match the behavior in 1.